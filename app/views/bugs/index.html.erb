<div class="container">
  <h1>検索結果一覧</h1>
  <div class="row">
    <%= if @bugs.present? %>
      <%= render @bugs %>
    <% else %>
      <p>検索条件マッチする虫がいません</p>
    <% end %>
  </div>
  <%= paginate @bugs %>
</div>
