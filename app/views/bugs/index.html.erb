<% content_for(:title, '不快な虫一覧') %>
<div class="bug_index pt-3"> 
  <div class="container">
    <h1 class="mb-4 pb-2 border-bottom border-dark">不快な虫たち一覧</h1>
    <div class="row">
      <% if @bugs.present? %>
        <%= render @bugs %>
      <% else %>
        <p>検索条件にマッチする虫がいません</p>
      <% end %>
      <%= paginate @bugs %>
    </div>
  </div>
</div>
