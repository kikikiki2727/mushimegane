<ul class="nav nav-tabs" id="myTab" role="tablist">
  <li class="nav-item" role="presentation">
    <a class="nav-link active" id="home-tab" data-bs-toggle="tab" href="#feature" role="tab" aria-controls="home" aria-selected="true">
      <%= Bug.human_attribute_name(:feature) %>
    </a>
  </li>
  <li class="nav-item" role="presentation">
    <a class="nav-link" id="profile-tab" data-bs-toggle="tab" href="#approach" role="tab" aria-controls="profile" aria-selected="false">
      <%= Bug.human_attribute_name(:approach) %>
    </a>
  </li>
  <li class="nav-item" role="presentation">
    <a class="nav-link" id="contact-tab" data-bs-toggle="tab" href="#prevention" role="tab" aria-controls="contact" aria-selected="false">
      <%= Bug.human_attribute_name(:prevention) %>
    </a>
  </li>
  <li class="nav-item" role="presentation">
    <a class="nav-link" id="contact-tab" data-bs-toggle="tab" href="#harm" role="tab" aria-controls="contact" aria-selected="false">
      <%= Bug.human_attribute_name(:harm) %>
    </a>
  </li>
</ul>
<div class="tab-content mt-4" id="myTabContent">
  <div class="tab-pane fade show active" id="feature" role="tabpanel" aria-labelledby="home-tab">
    <% if bug.feature.present? %>
      <p><%= safe_join(bug.feature.split("\n"), tag(:br)) %></p>
    <% else %>
      <p>「特徴」が登録されていません。</p>
    <% end %>
  </div>
  <div class="tab-pane fade" id="approach" role="tabpanel" aria-labelledby="profile-tab">
    <% if bug.approach.present? %>
      <p><%= safe_join(bug.approach.split("\n"), tag(:br)) %></p>
    <% else %>
      <p>「駆除方法」が登録されていません。</p>
    <% end %>
  </div>
  <div class="tab-pane fade" id="prevention" role="tabpanel" aria-labelledby="contact-tab">
    <% if bug.prevention.present? %>
      <p><%= safe_join(bug.prevention.split("\n"), tag(:br)) %></p>
    <% else %>
      <p>「予防方法」が登録されていません。</p>
    <% end %>
  </div>
  <div class="tab-pane fade" id="harm" role="tabpanel" aria-labelledby="contact-tab">
    <% if bug.harm.present? %>  
      <p><%= safe_join(bug.harm.split("\n"), tag(:br)) %></p>
    <% else %>
      <p>「実害」が登録されていません。</p>
    <% end %>
  </div>
</div>
